swagger: "2.0"
info:
  title: Trade Shop
  version: 0.1.0
produces:
- application/json
consumes:
- application/json
schemes:
- http
paths:
  /login:
    $ref: './post_login.yaml'
  /user/logout:
    $ref: './logout.yaml'
  /user/inventory:
    $ref: './user_inventory.yaml'
  /user/sale:
    $ref: './post_sale.yaml'
  /sales:
    $ref: './sales.yaml'
  /sales/{sale_id}/buy:
    $ref: './buy.yaml'
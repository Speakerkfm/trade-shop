get:
  tags:
  - sales
  description: Купить лот
  operationId: buy
  responses:
    200:
      description: OK
    401:
      $ref: ./responses/response_401.yaml
  parameters:
    - $ref: './parameter/sale_id.yaml'

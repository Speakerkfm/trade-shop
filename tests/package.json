{
  "name": "trade-shop-api-tests",
  "description": "testing api",
  "dependencies": {
    "amqplib": "^0.5.3",
    "hippie": "^0.5.2",
    "hippie-swagger": "^3.2.0",
    "mocha": "^6.0.2",
    "mocha-steps": "^1.3.0",
    "mysql": "^2.16.0",
    "redis": "^2.8.0",
    "swagger-parser": "^6.0.5",
    "util": "^0.11.1",
    "yamljs": "^0.3.0",
    "fs": "^0.0.1-security",
    "request": "^2.88.0",
    "chai": "^4.2.0"
  },
  "scripts": {
    "test": "mocha -b --timeout 20000 --recursive --exit --colors --require mocha-steps --grep @prepare --invert"
  }
}
